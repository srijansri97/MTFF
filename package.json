{
  "name": "raakaa-mtf-sentiment-apk",
  "version": "1.0.0",
  "description": "Raakaa Capital - Multi-Timeframe Sentiment Speedometers (Android)",
  "private": true,
  "scripts": {
    "android:add": "cordova platform add android",
    "build": "cordova build android",
    "build:release": "cordova build android --release"
  },
  "devDependencies": {
    "cordova": "^12.0.0",
    "cordova-android": "^13.0.0"
  },
  "cordova": {
    "platforms": [
      "android"
    ]
  }
}
